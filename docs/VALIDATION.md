# 打包验证

本次整理版本为 1.1.2.10。`source-snapshot.json` 记录了部署版本的源码校验值；前端与后端源码复制后逐一核对一致。

- 仓库内 arm64 优化编译通过；无需临时目录、Homebrew 绝对头文件路径或原用户名。
- 从仓库重新生成标准 macOS 安装包，应用签名校验、安装位置与不可迁移组件配置检查通过。
- 安装包的 SHA-256 文件使用相对文件名，在 `dist/` 内执行 `shasum -a 256 -c Squirrel-Ghost-1.1.2.10.pkg.sha256` 可验证。
- 前端 5 套隔离测试通过，包括 880 组上下文预算、512 次滚动接受、history/document-history、全部列行坐标与异步提交、删除、布局变化回归。
- 后端 15 项单元测试通过；需要真实 tokenizer 的检查默认跳过。默认测试不加载模型。
- 安装脚本只执行了检查模式；本次整理未安装、重载输入法、启动服务或下载模型。应用中的实际输入体验不属于此次打包验证。

常用验证命令见根目录和 `backend/`、`tests/frontend/` 的 README。
